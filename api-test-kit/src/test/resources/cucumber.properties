# Specify the glue path for step definitions
cucumber.glue=petstore_api.bdd.stepdefinitions

# Specify the feature file location
cucumber.features=src/test/java/petstore_api/bdd/features

# Enable strict mode (fail if there are undefined or pending steps)
cucumber.strict=true

# Set the default plugin (e.g., pretty output)
cucumber.plugin=pretty